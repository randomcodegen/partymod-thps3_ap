[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "thps3-apclient"
version = "1.0.2"
description = "Native Archipelago client and game bridge tooling for Tony Hawk's Pro Skater 3"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
